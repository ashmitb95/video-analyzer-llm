# Gemini — watches the video to pick frames and produce whole-video analysis.
# The only key screenscribe needs. Transcript-only mode needs no key at all.
# Get one at https://aistudio.google.com/apikey
GEMINI_API_KEY=
