{
  "type": "object",
  "description": "The final, end-state configuration file or code file produced in the video.",
  "properties": {
    "filename": { "type": "string", "description": "The file's name if shown, else a best guess" },
    "language": { "type": "string", "description": "Language or format (e.g. yaml, json, python)" },
    "content": { "type": "string", "description": "The complete final file contents" }
  },
  "required": ["content"]
}
